[project]
name = "time-with-mom"
version = "0.1.0"
description = "Monte Carlo simulation for long-term family time"
authors = [{name="Sijie Yang"}]
dependencies = ["numpy"]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"